arch: "aarch64"

images:
  - location: "https://cloud-images.ubuntu.com/jammy/current/jammy-server-cloudimg-arm64.img"
    arch: "aarch64"

cpus: 8
memory: "8GiB"
disk: "150GiB"

vmOpts:
  vz:
    rosetta:
      enabled: true
      binfmt: true

mounts:
  - location: "/Volumes/Kernel"
    writable: true

provision:
  - mode: system
    script: |
      set -eux

      # --- Fix ARM repo ---
      cat > /etc/apt/sources.list <<'EOF'
      deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports jammy main universe restricted multiverse
      deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports jammy-updates main universe restricted multiverse
      deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports jammy-backports main universe restricted multiverse
      deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports jammy-security main universe restricted multiverse
      EOF

      # --- Enable amd64 ---
      dpkg --add-architecture amd64

      # --- Add amd64 repo ---
      cat > /etc/apt/sources.list.d/amd64.list <<'EOF'
      deb [arch=amd64] http://archive.ubuntu.com/ubuntu jammy main universe restricted multiverse
      deb [arch=amd64] http://archive.ubuntu.com/ubuntu jammy-updates main universe restricted multiverse
      deb [arch=amd64] http://archive.ubuntu.com/ubuntu jammy-backports main universe restricted multiverse
      deb [arch=amd64] http://security.ubuntu.com/ubuntu jammy-security main universe restricted multiverse
      EOF

      # --- Clean + update ---
      apt-get clean
      rm -rf /var/lib/apt/lists/*
      apt-get update

      # --- Install amd64 runtime libs ---
      apt-get install -y \
        libc6:amd64 \
        libstdc++6:amd64 \
        zlib1g:amd64 \
        libgcc-s1:amd64 \
        file \
        curl

      # --- Fix dynamic linker for Rosetta ---
      mkdir -p /lib64
      ln -sf /lib/x86_64-linux-gnu/ld-linux-x86-64.so.2 /lib64/

      # --- Install Java 21 ---
      apt-get install -y openjdk-21-jdk

      # --- Install Python 3 and pip3 ---
      apt-get install -y python3 python3-pip python3-venv

  - mode: user
    script: |
      set -eux

      # --- Set Env Vars ---
      echo 'export OVERRIDE_BIN_ARCH="amd64"' >> ~/.bashrc
      echo 'export BUILD_USER="Exodus"' >> ~/.bashrc
      echo 'export BUILD_HOST="Darwin"' >> ~/.bashrc

      # --- Copy repo from mounted volume ---
      cp -r /Volumes/Kernel/HyperOS-Port-Python ~/

      # --- Install Python dependencies ---
      cd ~/HyperOS-Port-Python
      pip3 install -r requirements.txt

# images:
#   - location: "https://cloud-images.ubuntu.com/noble/current/noble-server-cloudimg-arm64.img"
#     arch: "aarch64"

# provision:
#   - mode: system
#     script: |
#       set -eux

#       # --- Fix ARM repo (deb822) ---
#       cat > /etc/apt/sources.list.d/ubuntu.sources <<'EOF'
#       Types: deb
#       URIs: http://ports.ubuntu.com/ubuntu-ports
#       Suites: noble noble-updates noble-backports
#       Components: main universe restricted multiverse
#       Architectures: arm64
#       Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg

#       Types: deb
#       URIs: http://ports.ubuntu.com/ubuntu-ports
#       Suites: noble-security
#       Components: main universe restricted multiverse
#       Architectures: arm64
#       Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg
#       EOF

#       # --- Enable amd64 ---
#       dpkg --add-architecture amd64

#       # --- Add amd64 repo (deb822) ---
#       cat > /etc/apt/sources.list.d/amd64.sources <<'EOF'
#       Types: deb
#       URIs: http://archive.ubuntu.com/ubuntu
#       Suites: noble noble-updates noble-backports
#       Components: main universe restricted multiverse
#       Architectures: amd64
#       Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg

#       Types: deb
#       URIs: http://security.ubuntu.com/ubuntu
#       Suites: noble-security
#       Components: main universe restricted multiverse
#       Architectures: amd64
#       Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg
#       EOF

#       # --- Clean + update ---
#       apt-get clean
#       rm -rf /var/lib/apt/lists/*
#       apt-get update

#       # --- Install amd64 runtime libs ---
#       apt-get install -y \
#         libc6:amd64 \
#         libstdc++6:amd64 \
#         zlib1g:amd64 \
#         libgcc-s1:amd64 \
#         file \
#         curl

#       # --- Fix dynamic linker for Rosetta ---
#       mkdir -p /lib64
#       ln -sf /lib/x86_64-linux-gnu/ld-linux-x86-64.so.2 /lib64/

#       # --- Install Java 21 ---
#       apt-get install -y openjdk-21-jdk

#       # --- Install Python 3 and pip3 ---
#       apt-get install -y python3 python3-pip python3-venv
